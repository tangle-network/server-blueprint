[
  {
    "config": {
      "runtime": "docker",
      "package": "mcp/redis",
      "args": [],
      "env": [
        ["REDIS_HOST", "localhost"],
        ["REDIS_PORT", "6379"]
      ]
    }
  }
]
